# 按活跃主线可达性清理代码与测试

重构后的 main 只保留唯一基础 Emo-FiLM 数据准备、训练、推理、正式本地评测及其必要工程门禁。凡只服务旧诊断矩阵、消融、DPO、PA/RL、废弃兼容路径或已取消指标的 tools、tests、配置、适配器和辅助代码均从活跃主线删除，不因体积小或曾经使用过而保留；历史代码通过 `main@5ad481d`、`feature/emofilm-author-model-control@e265281` 的 Git tag/bundle，以及文档和结论证据包恢复。实际批量删除仍须在实施前列出精确文件清单并按危险操作规则确认。
